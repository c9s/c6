#include <stdlib.h>
#include "gtest/gtest.h"

TEST(CompositionOptionTest, Constructor) {
    // CompositeOptions o(52, 68, 10, RightBottom);
    // o.set_thumb_resize_width(500);
}

TEST(CompositionOptionTest, AddColorsFromImage) {

}

int main(int argc, char **argv) {
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
